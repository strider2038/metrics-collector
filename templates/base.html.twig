<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% if pageTitle is defined %} {{ pageTitle }} | {% endif %} {{ appTitle }}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
